#include "main.h"
#include <string.h>

/**
 * _memset -> this memory set fonction
 * @s: string
 * @b: a character
 * @n: an integer
 * Return: a string
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
return (memset(s,b,n));
}
